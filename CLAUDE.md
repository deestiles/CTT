# Claude project instructions

Read `PROJECT_GUIDE.md` completely before planning or editing. It is the shared source of truth for the user, Claude, and ChatGPT/Codex.

Do not replace the current map/lane foundation or Polygon City integration. Preserve existing work, inspect `git status` and `git diff`, work on a focused branch, test in Godot, and update the Current Status / Handoff Notes in `PROJECT_GUIDE.md` after material changes.
