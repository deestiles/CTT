# AI working agreement

Before changing this project, read `PROJECT_GUIDE.md` completely and follow it as the canonical technical and design specification.

Preserve existing user work. Inspect `git status` and `git diff` before editing, make a focused branch, and never discard unrelated changes. After material work, update the Current Status / Handoff Notes section in `PROJECT_GUIDE.md` so Codex and Claude receive the same context.
